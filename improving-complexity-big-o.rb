1. Big-O of version one (insertion sort):
		-O(n^2)

2. Big-O of version two (merge sort):
		-O(n log(n))

3. Big-O of version three(heap sort):
		-O(n log(n))  (same Big-O time complexity as merge sort, but has better space complexity (O(1)=constant)
